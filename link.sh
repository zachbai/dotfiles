CURRENT_DIRECTORY=$(pwd)

ln -fs $CURRENT_DIRECTORY/shell $HOME/shell
ln -fs $CURRENT_DIRECTORY/zshrc $HOME/.zshrc
ln -fs $CURRENT_DIRECTORY/tmux.conf $HOME/.tmux.conf
ln -fs $CURRENT_DIRECTORY/tmux $HOME/.tmux
ln -fs $CURRENT_DIRECTORY/vimrc $HOME/.vimrc
ln -fs $CURRENT_DIRECTORY/vim $HOME/.vim
ln -fs $CURRENT_DIRECTORY/nvim $HOME/.config/
